<div class="container">
  <div class="row">
    <div class="col-3"></div>
    <div class="col-8">
      <h1>Book dateil</h1>
      <table class="table">
        <thead>
          <tr>
            <td><%= @book.title %></td>
            <td><%= @book.body %></td>
            <td><%= link_to "edit", edit_book_path(@book) %></td>
            <td><%= link_to "destroy", book_path(@book), method: :delete %></td>
          </tr>
        </thead>
      </table>
    </div>
  </div>
</div>